using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Ubiq.Samples
{
    /// <summary>
    /// Manages a set of icons above a remote avatar GameObject, to inform the local player about that avatar's state.
    /// Status indicators use a "pull" model - interrogating the Scene for information about the remote player.
    /// </summary>
    public class AvatarStatusIndicator : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}